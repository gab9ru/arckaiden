{
  "version": "0.2.0",
  "configurations": [
  
    {
      "name": "Open index.html in Edge",
      "type": "pwa-msedge",
      "request": "launch",
      "file": "${workspaceFolder}/index.html",
      "webRoot": "${workspaceFolder}"
    }
  ]
}